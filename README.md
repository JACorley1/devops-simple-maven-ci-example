#DevOps Simple Maven CI Example
This repos provides an example of a continuous integration pipeline established using Github Actions for a Maven project. The repos also has branch protection rules to enforce that all changes in release branch must use a pull request.
